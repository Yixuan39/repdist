% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{read_embeddings}
\alias{read_embeddings}
\title{Read pre-computed protein embeddings}
\usage{
read_embeddings(path, embedding_key = NULL, id_key = NULL)
}
\arguments{
\item{path}{Path to a NumPy \code{.npz} archive.}

\item{embedding_key}{Name of the embedding array. \code{NULL} detects
\code{embeddings}, then \code{embedding}.}

\item{id_key}{Name of the protein-identifier array. \code{NULL} detects \code{ids},
\code{headers}, then \code{accession}.}
}
\value{
Numeric matrix with protein identifiers as row names.
}
\description{
Imports a NumPy \code{.npz} archive produced outside R and returns the same matrix
shape as \code{\link[=embed_proteins]{embed_proteins()}}: proteins in rows and embedding dimensions in
columns. By default, \code{embeddings} or \code{embedding} identifies the
two-dimensional numeric array, and \code{ids}, \code{headers}, or \code{accession}
identifies the matching one-dimensional protein identifier array.
}
\details{
NumPy object arrays are deliberately rejected because loading them requires
Python pickle. Save identifiers as a Unicode array, for example
\code{numpy.asarray(ids, dtype=str)}.
}
\examples{
if (interactive()) {
    Z <- read_embeddings("protein_embeddings.npz")
    sample_repdist(counts, Z)
}
}
