% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binning.R
\name{repdist_bin}
\alias{repdist_bin}
\title{Cluster proteins into structural bins}
\usage{
repdist_bin(counts, embeddings, cluster_threshold = 0.5, seqs = NULL)
}
\arguments{
\item{counts}{Integer matrix of counts, or relative abundances, samples in
rows, proteins in columns.}

\item{embeddings}{Numeric matrix, one row per protein, rownames matching
\code{colnames(counts)} -- or a precomputed protein \code{dist} from \code{\link[=seq_repdist]{seq_repdist()}}.
Must be cosine-calibrated to a similarity score for \code{cluster_threshold} to
mean anything.}

\item{cluster_threshold}{Similarity score defining the bin boundary
(default 0.5, TM-score's same-fold threshold). A vector cuts the same
dendrogram at each value.}

\item{seqs}{Optional named character vector or \link[Biostrings:AAStringSet]{Biostrings::AAStringSet} of
protein sequences. It must contain every protein in \code{counts}.}
}
\value{
A list with two elements:
\describe{
\item{\code{bins}}{For a scalar threshold, a list containing \code{counts} (samples
x bins), \code{bin} (protein to bin membership), \code{bin_dist} (distance between
representative proteins), and \code{reps} (bin medoids and sizes). For a
threshold vector, a named list of these objects.}
\item{\code{representative_sequences}}{A bin-named
\link[Biostrings:AAStringSet]{Biostrings::AAStringSet} containing each medoid sequence, or \code{NULL} if
\code{seqs} was not supplied. For a threshold vector, a named list of these
objects.}
}
}
\description{
Collapses \code{counts} from protein-level to structural-bin level by clustering
\code{embeddings} and summing abundance within each bin -- the structural
analogue of collapsing an ASV table to a coarser rank. Point this at a
per-bin differential-abundance test (corncob, ALDEx2, ANCOM-BM, ...)
instead of comparing raw proteins one at a time.
}
\details{
Cosine similarity between TM-Vec vectors is the predicted TM-score, so
\code{1 - cluster_threshold} is a distance cutoff. Clustering is complete
linkage, so every pair within a bin meets the threshold, not just nearest
neighbors. Default 0.5 is TM-score's conventional same-fold boundary (Xu &
Zhang, Bioinformatics 2010).

Pass a vector of thresholds to compare several cuts. They share one pairwise
distance and one dendrogram. Complete linkage defines bin membership so every
protein pair in a bin meets the requested TM-score threshold.

\code{bin_dist} is the cosine distance between bin representatives. For
bin-resolution MMD, index the original embedding matrix by
\code{out$bins$reps$protein}, rename those rows with \code{out$bins$reps$bin}, and pass
them with \code{out$bins$counts} to \code{\link[=sample_repdist]{sample_repdist()}}.

Cost: the ground distance contains n(n-1)/2 doubles (roughly 400 MB at
10,000 proteins). It remains condensed through clustering and medoid
selection. Pass a precomputed \code{\link[=seq_repdist]{seq_repdist()}} as \code{embeddings} to build it
once across calls.
}
\examples{
Z <- rbind(p1 = c(1, 0), p2 = c(0.95, 0.05), p3 = c(0, 1))
counts <- rbind(s1 = c(5, 3, 0), s2 = c(0, 2, 7))
colnames(counts) <- rownames(Z)
repdist_bin(counts, Z)
}
