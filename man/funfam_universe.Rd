% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repdist-package.R
\docType{data}
\name{funfam_universe}
\alias{funfam_universe}
\title{CATH FunFam protein universe}
\format{
A list with \code{members} (1500 rows: protein identifier, family, label),
\code{embeddings} (1500 by 512, historical scikit-bio/tmvec coordinates rounded
to 4 decimals), \code{sequences} (named domain amino-acid strings), and
\code{provenance} (source, selection parameters and model identity).
Member identifiers match embedding row names and sequence names in order.
Renormalize rounded rows before computing cosine distances.
}
\source{
CATH v4.3 FunFam Stockholm alignments, accessed 2026-08-23.
\url{https://www.cathdb.info/}. CATH database content: CC BY 4.0.
}
\description{
Ten CATH v4.3 functional families with 150 domains each, used for
simulation. Stored as \code{extdata/funfam_universe.rds}; load with
\code{readRDS()}.
}
\details{
The historical head differs from the current default \code{tmvec}.
Original model revisions were not recorded; these fixed coordinates are
an example, not a bitwise inference reference. The installed
\code{scripts/DATA_PROVENANCE.md} describes recovery of the exact
sequence set and recomputation using a pinned version of the historical
head.
}
\examples{
u <- readRDS(system.file("extdata", "funfam_universe.rds",
    package = "repdist"
))
table(u$members$label)
}
