% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{known_models}
\alias{known_models}
\title{Models \code{embed_proteins()} assembles from more than one HuggingFace repo}
\usage{
known_models()
}
\value{
Named list, one entry per known head repo, each with \code{head} (the
architecture to build) and \code{backbone} (the repo to run underneath it).
}
\description{
A trained head and the frozen backbone it was trained on are two separate
repos, and the head's own \code{config.json} does not record which backbone it
expects. That pairing lives in \code{inst/extdata/models.json} -- add an entry
there to teach \code{\link[=embed_proteins]{embed_proteins()}} a new head, no code change needed.
}
