% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distance.R
\name{weightedRep}
\alias{weightedRep}
\title{Weighted-representation distance between samples}
\usage{
weightedRep(counts, embeddings, method = c("mmd", "rbf_mmd"), sigma = NULL)
}
\arguments{
\item{counts}{Integer matrix of counts, or relative abundances, samples in
rows, proteins in columns.}

\item{embeddings}{Numeric matrix, one row per protein, rownames matching
\code{colnames(counts)}.}

\item{method}{"mmd" (linear MMD) or "rbf_mmd".}

\item{sigma}{RBF bandwidth. Defaults to the median heuristic.}
}
\value{
A \code{dist} object.
}
\description{
Treats each sample as its abundance-weighted mean over protein
representations, and returns an MMD-family distance between samples,
ready for \code{vegan::adonis2()}.
}
\details{
\code{"mmd"} is the Euclidean distance between weighted mean representations
(linear kernel). \code{"rbf_mmd"} swaps in a median-heuristic RBF kernel, which
also picks up higher-order differences in how mass is spread out, not just
the mean.

Rarefy \code{counts} first (e.g. \code{vegan::rrarefy()}) if samples weren't
collected at a common depth.
}
