% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QC.R
\name{length_abundance_profile}
\alias{length_abundance_profile}
\title{Proportion of proteins, and of abundance, longer than each length}
\usage{
length_abundance_profile(counts, lengths, max_length = NULL)
}
\arguments{
\item{counts}{Matrix of counts or relative abundances, samples in rows,
proteins in columns. Every row must have a positive total.}

\item{lengths}{Named numeric vector of protein lengths, or a named character
vector of sequences (lengths via \code{nchar()}); names must cover
\code{colnames(counts)}.}

\item{max_length}{Optional length to mark with a vertical reference line.}
}
\value{
A ggplot object. Needs the \code{ggplot2} package.
}
\description{
Two survival curves on a shared length axis: the fraction of the protein
catalog, and the fraction of total abundance, longer than each x. Reading
both at a candidate cutoff -- an embedding model's training range, say --
shows how much catalog and abundance lie beyond it.
}
\details{
\code{counts} is converted to per-sample relative abundance first, so the
abundance curve reflects length rather than sequencing depth. Rarefied input
is not required.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(1)
  lengths <- stats::setNames(round(exp(rnorm(100, log(300), 0.6))) + 20,
                             paste0("p", 1:100))
  counts <- matrix(rpois(400, 5) + 1L, 4, 100,
                   dimnames = list(paste0("s", 1:4), names(lengths)))
  length_abundance_profile(counts, lengths, max_length = 1000)
}
}
