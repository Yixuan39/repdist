% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc.R
\name{plot_similarity_profile}
\alias{plot_similarity_profile}
\title{Plot the distribution of nearest-neighbour protein similarity}
\usage{
plot_similarity_profile(D, min_sim = NULL)
}
\arguments{
\item{D}{Protein \code{dist} from \code{\link[=repdist_matrix]{repdist_matrix()}}, or a square matrix on the
same \code{1 - similarity} scale. A euclidean \code{dist} is rejected.}

\item{min_sim}{Optional binning threshold to mark with a vertical line.}
}
\value{
A ggplot object. Needs the \code{ggplot2} package.
}
\description{
Histogram of each protein's similarity to its nearest neighbour in the
ground metric -- for TM-Vec embeddings, a predicted TM-score. A pile-up
at 1 means near-duplicate proteins, or an embedding model too saturated to
resolve similarity thresholds; every protein left of a candidate \code{min_sim}
becomes a singleton bin in \code{\link[=bin_proteins]{bin_proteins()}}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  set.seed(1)
  Z <- matrix(rnorm(200), 50, 4, dimnames = list(paste0("p", 1:50), NULL))
  plot_similarity_profile(repdist_matrix(Z), min_sim = 0.7)
}
}
