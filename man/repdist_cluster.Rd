% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/binning.R
\name{repdist_cluster}
\alias{repdist_cluster}
\title{Cluster proteins by structural similarity}
\usage{
repdist_cluster(
  embeddings,
  cluster_threshold = 0.5,
  method = c("complete", "dbscan", "hdbscan"),
  min_size = 5L
)
}
\arguments{
\item{embeddings}{Numeric matrix, one row per protein, or a precomputed
protein \code{dist} from \code{\link[=seq_repdist]{seq_repdist()}}.}

\item{cluster_threshold}{Similarity score defining the bin boundary. Used by
\code{"complete"} and \code{"dbscan"}; ignored by \code{"hdbscan"}.}

\item{method}{\code{"complete"} (complete-linkage hierarchical clustering at one
global cut), \code{"dbscan"} (density-based at one global radius), or
\code{"hdbscan"} (density-based, each cluster keeping its own radius).}

\item{min_size}{Minimum points forming a dense region, for the two density
methods.}
}
\value{
A named integer vector of cluster ids, one per protein.
}
\description{
Returns bin membership without collapsing an abundance table. This is the
clustering step of \code{\link[=repdist_bin]{repdist_bin()}}, exposed for benchmarking a partition
against a reference grouping.
}
\examples{
Z <- rbind(p1 = c(1, 0), p2 = c(0.95, 0.05), p3 = c(0, 1))
repdist_cluster(Z)
}
