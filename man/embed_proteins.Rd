% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{embed_proteins}
\alias{embed_proteins}
\title{Embed protein sequences with a HuggingFace protein language model}
\usage{
embed_proteins(
  seqs,
  model = "scikit-bio/tmvec-swissmodel",
  device = c("mps", "cuda", "cpu"),
  batch_size = 16,
  token_budget = 16384L
)
}
\arguments{
\item{seqs}{Named character vector of amino-acid sequences, \strong{or} a path to a
FASTA file (see \code{\link[=read_fasta]{read_fasta()}}).}

\item{model}{HuggingFace repo. Either a head listed in \code{\link[=known_models]{known_models()}} (the
default TM-Vec1 head was trained on chains up to 300 residues; longer
sequences still embed but are out of the training range), or any encoder
repo, embedded generically.}

\item{device}{"mps", "cuda", or "cpu".}

\item{batch_size}{Maximum sequences per forward pass. The real limit is
\code{token_budget}; this caps the short-sequence end.}

\item{token_budget}{Maximum batch x padded-length product per forward pass.
A hardware knob, not a model property -- lower it if the device runs out
of memory, raise it to use a bigger card more fully.}
}
\value{
Numeric matrix, one row per sequence, rownames = names(seqs).
}
\description{
Dispatches on \code{model}. A repo listed in \code{\link[=known_models]{known_models()}} is assembled with
its registered head over its registered backbone -- by default TM-Vec1,
whose head regresses the TM-score of protein pairs, so cosine similarity
between two vectors is a predicted TM-score (see
\code{docs/ground_metric_models.md}).
}
\details{
Any other repo is embedded directly: loaded with \code{AutoModel}/\code{AutoTokenizer}
and represented by the mean-pooled last hidden state. No calibration claim
-- unlike TM-Vec1, cosine similarity there isn't a named, verified quantity.

Downloads are cached under \verb{~/.cache/huggingface}.

Needs \code{torch}, \code{transformers}, \code{sentencepiece}, \code{safetensors} and
\code{huggingface_hub} importable from the Python reticulate is using (see
\code{reticulate::py_config()}). Errors with the exact \verb{pip install} command if
any are missing -- install them yourself rather than relying on this
function to provision an environment.
}
